<!DOCTYPE html>
<html>
  <head>
    <title>Langage</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <link rel="stylesheet" href="style.css">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <center>
    <% if notice %>
      <%= notice %>
    <% end %>
    </center>
       
<!-- <header>

<script>
  $(document).ready(function(){
      $("span.navbar-toggler-icon").click(function(){
          $("#navbarHeader").toggle();
      });
  });
</script>

    <div class="collapse bg-dark" id="navbarHeader">
      <div class="container">
        <div class="row">
          <div class="col-sm-8 col-md-12 py-4">
             <% if cookies[:saved_subscriber] %>
              <center>
                    <h1 class="text-white">Merci pour ton inscription !!!</h1>
              </center>
              <% else %>
              <center>
                    <h1 class="text-white">Inscrit toi ici !!!</h1>
              </center>
                <%= form_for Subscriber.new, remote: true, html: {class:"form-inline justify-content-center" } do |f| %>
                  <%= f.text_field :name, placeholder: "name", class:"form-control mb-2 mr-sm-2" %>
                  <%= f.email_field :email, placeholder: "email", class:"form-control mb-2 mr-sm-2" %>
                  <div  class="mb-2 mr-sm-2">
                  <%= f.select :langage, ["Ruby","Html","Css","Javascript","bootstrap","GitHub"],prompt: "Langage de programmation" %>
                  </div>
                  <div  class="mb-2 mr-sm-2">
                    <%= f.country_select :pays ,prompt: "Dans quelle pays es-tu ?" %>
                  </div>
                    <%= f.submit "S'inscrire !!!", class:"btn btn-warning mb-2 mr-sm-2"%>
                <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div class="navbar navbar-dark bg-dark box-shadow">
      <div class="container d-flex justify-content-between">
        <a href="<%= root_path %>" class="navbar-brand d-flex align-items-center">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="mr-2"><path d="M23 19a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h4l2-3h6l2 3h4a2 2 0 0 1 2 2z"></path><circle cx="12" cy="13" r="4"></circle></svg>
          <strong>Langage</strong>
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>
    </div>
  </header> -->
    
      <%= yield %>
  <script src="jquery.min.js"></script>
  </body>
</html>
